{
  "name": "@flowstile/ui",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "build": "echo 'placeholder — no build yet'",
    "dev": "echo 'placeholder — no dev server yet'",
    "test": "echo 'placeholder — no tests yet'"
  }
}
